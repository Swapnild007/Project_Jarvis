export const gates=['moduleCoverage','sharedState','dependencyIntegrity','security','build','deployment'];
export function auditIntegration(state={}){const modules=state.modules||[];const implemented=state.implementedModules||[];const required=new Set(modules.map(m=>m.id||m[0]));const done=new Set(implemented);const missing=[...required].filter(id=>!done.has(id));const checks={moduleCoverage:missing.length===0,sharedState:Boolean(state.sharedState),dependencyIntegrity:Boolean(state.dependencyIntegrity),security:Boolean(state.securityAudit),build:Boolean(state.buildPassed),deployment:Boolean(state.deploymentReady)};return {checks,missing,passed:Object.values(checks).filter(Boolean).length,total:gates.length,productionReady:Object.values(checks).every(Boolean),auditedAt:new Date().toISOString()};}
export function productionDecision(audit){return audit?.productionReady?{status:'GO',reason:'All production gates passed.'}:{status:'BLOCKED',reason:'One or more production gates remain incomplete.',failed:gates.filter(g=>!audit?.checks?.[g])};}
